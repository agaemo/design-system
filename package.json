{
  "name": "@agaemo/design-system",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run build:tokens && vite",
    "build": "bun sd.config.ts && mkdir -p docs/css && cp css/variables.css docs/css/variables.css && cp css/components.css docs/css/components.css && vite build",
    "build:tokens": "bun sd.config.ts && mkdir -p docs/css && cp css/variables.css docs/css/variables.css && cp css/components.css docs/css/components.css",
    "lint": "oxlint .",
    "format": "oxfmt ."
  },
  "devDependencies": {
    "oxfmt": "^0.56.0",
    "oxlint": "^1.71.0",
    "style-dictionary": "^5.5.0",
    "vite": "7"
  }
}
